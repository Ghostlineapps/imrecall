// Placeholder — rigenera con:
//   npx supabase gen types typescript --project-id <PROJECT_ID> > src/lib/supabase/types.ts
// dopo aver applicato le migrazioni in supabase/migrations/.
//
// Nel frattempo usiamo `any` per non bloccare lo sviluppo: i tipi verranno
// stretti automaticamente non appena il progetto Supabase è collegato.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
